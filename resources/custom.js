$ ( document ).ready ( function ( ) {
	console.log ("DOM is ready")
	
	$('#name').keyup( function ( ) {
		// var searchField = $('#name').val();
		// var myExp = new RegExp (searchField, 'i');
		// $.getJSON ('users.json',function (data) {
		console.log ("Typing")	
		})
	})

	// $.get ( '/form', ajaxdata, function ( data ) {
	// 	console.log ( data )
	// 	$ ( '#name' ).text (data.firstname)
	// })
})